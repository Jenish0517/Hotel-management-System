# Student-Management-System-
It allows users to add, update, remove, search, and list students. Student details such as ID, name, age, and course are stored in an ArrayList. The program uses classes, objects, constructors, and methods. User input is given through Scanner and controlled by a while loop menu. CRUD operations are performed using if else statements.
